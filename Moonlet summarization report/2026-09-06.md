# PR Activity Summary — 2026-09-06

## Merged
- **#36 Orbio account keys, follow-ups, memory between runs, GitHub token probe** (capy/onboarding, merged 12:46 UTC). Squashes work since #31, live on 16labs.xyz.
  - New `src/moonlet/llm.ts`: agent loop over chat/completions on orbio.so/api/v1 (sk-orbio- keys) or OpenRouter (sk-or- keys); `@openrouter/agent` dropped at runtime.
  - One Orbio key per wallet shared across all its moonlets; minting serialized per owner (`withOwnerLock`) — verified 20 concurrent moonlets produce 1 mint, no races.
  - Legacy OpenRouter keys folded into Orbio balance; deleting a moonlet doesn't touch credits.
  - Local `web_fetch` tool added.
  - Follow-up commits under same merge: cadence changes notify owner when income slows the schedule; Telegram image sniffing fix (file server returns octet-stream, so bytes are now sniffed for type); repo jobs park quietly without GitHub instead of surfacing 404s; revoked GitHub tokens now detected and the owner notified; isolated scheduler test DB; a holder's moonlets now share one Orbio key with rotations propagating to siblings; friendly errors on rejected keys; heuristic checks added to the no-model plan; Telegram delivery failures now logged; reply-to and schedule-from-plain-words follow-ups added.
- **#31 Capy/onboarding** (merged earlier, no PR description).

## Closed, no-op
- PRs #29, #30, #32–#35, #37–#43: all titled "test: moonlet PR round-trip (auto-closed)", opened and closed automatically by the connections test suite to verify a moonlet can open a PR. No code changes, no review needed.

## Notes
- No open PRs at time of check.
- Next check due ~2026-09-07T02:31 UTC (12h cadence).
